query CartValidationsGenerateRunInput {
  cart {
    lines {
      quantity
      merchandise {
        __typename
        ... on ProductVariant {
          product {
            id
            title
            hasTags(tags: ["quote-only"]) {
              tag
              hasTag
            }
            metafield(namespace: "quote", key: "quote_only") {
              value
            }
          }
        }
      }
    }
  }
}
